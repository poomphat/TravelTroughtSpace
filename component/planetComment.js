import React, { Component } from 'react';
import { StyleSheet, Text, View, Image } from 'react-native';

const PlanetComment = (props) =>{
  
    return (
        
 
    );
  }
export default PlanetComment;

const styles = StyleSheet.create({
    
  });